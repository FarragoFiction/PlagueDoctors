<!DOCTYPE html>
<html>
  <head>
    <title>PlagueDoctor</title>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag asset_path('favicon.ico'), :rel => 'icon', :type => "image/x-icon" %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script>
      function helloWorld(){
          alert("Hello World");
      }

      //not actually going to be used cuz can't do local storage shit across domains, derp
      function storeFile(fileString){
          var tag = "PALDEMICPREVIEWFILE";
          try{
              console.log("storing card");
              localStorage.setItem(tag,cardString);
          }catch(e){
              console.log("local storage not available for paldemic file preview storage");
          }
      }

    </script>
  </head>

  <body>
  <h1><span class = "logoName">Plague Doctors: Paldemic File Database</span></h1>

  <%= yield %>
  </body>
</html>
